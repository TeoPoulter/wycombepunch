# Website polish checks — 15 September 2026

## Verified locally in signed-in Chrome

- All seven HTML pages checked at 320, 390, 580, 800, 801, 1024, 1440 and 2560 CSS pixels. No page horizontal scrolling or visible text overflow after layout settled.
- Additional homepage audit covered 360, 600, 768, 820 and 1920 pixels.
- Homepage, game and booking information checked with root text size at 200% at 320, 801 and 1440 pixels. No horizontal scrolling or text overflow. This checks enlarged rem-based body text; it is not a claim of testing every browser zoom mode.
- Navigation at 800 × 360 fits the short landscape viewport and allows internal scrolling.
- Screenshots inspected for the desktop hero, phone hero/game/form, cabinet arrow and labels, punctuation spacing, and separated score artwork/badge.
- Full Punch Rush run completed through Enter, Space and pointer controls: hits 6 + 75 + 283 produced 364/999. Motion-free mode completed with explicit starts for all three hits.
- Three-step enquiry builder validated to an explicitly unsent draft. No live enquiry was transmitted.

## Automated checks

- `python3 tests/test_static.py`: 3 passed (all page links/IDs/images, both CNAME files, retired labels).
- `node --test tests/test_game.mjs`: 8 passed (scoring, linked hits, reduced motion, pause/reset, copy fallback).
- `cd backend && npm test`: 11 passed (UK midnight/DST, concurrent stored maximum, validation, client failure/stale-response handling).
- JavaScript syntax and `git diff --check` passed.

The older `tests/test_site.py` describes the original package, including former demo copy and scoring. Its expectations are historical; use the checks above for this revision.

## Remaining connections

The daily-score service is implemented and tested locally, but not deployed. Its endpoint remains blank and its UI is hidden. Cloudflare account access requires the owner's explicit approval. Do not claim a shared daily record is live.

Online enquiries remain unavailable until the owner supplies genuine operator/contact details, confirms the hire offer, and connects a submission route. No inbox delivery has been tested. The updated visitor copy accurately says drafts have not been sent.

GitHub Pages source remains `main` / `docs`, custom domain remains `wycombepunch.com`, and HTTPS enforcement remains enabled. DNS settings are unchanged.
